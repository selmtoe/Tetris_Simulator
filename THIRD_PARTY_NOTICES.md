# Third-party notices

## sfinder-cpp

The PC guide uses the C++ perfect-clear search core from `sfinder-cpp` by
knewjade. The bundled source is in `third_party/sfinder-cpp-master/` and is
licensed under the MIT License; see its included
[LICENSE](third_party/sfinder-cpp-master/LICENSE).

The browser-specific wrapper in `simulator/pc-solver/pc_solver_api.cpp` and
the Worker/UI integration are project code.
